package com.kfokam48.presence.domain;

/** Publié au premier enregistrement d'une présence : relance le tirage des exercices restés DEPOSE (H1). */
public record PresenceEnregistree(Presence presence) {
}
