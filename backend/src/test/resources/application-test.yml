# Profil test : H2 en mode PostgreSQL, aucune base locale requise (B6)
spring:
  datasource:
    url: jdbc:h2:mem:kf;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  flyway:
    locations: classpath:db/migration
